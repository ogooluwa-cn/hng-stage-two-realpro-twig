import type { Duration } from "luxon";
/**
 * @param {"luxon".luxon.Duration} duration
 * @param {string} format
 *
 * @returns {string} The formatted interval.
 */
export declare const formatDuration: (duration: Duration, format: string) => string;
