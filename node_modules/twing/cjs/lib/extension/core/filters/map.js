"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.mapSynchronously = exports.map = void 0;
const iterator_to_map_1 = require("../../../helpers/iterator-to-map");
const map = async (_executionContext, map, callback) => {
    const result = new Map();
    map = (0, iterator_to_map_1.iteratorToMap)(map);
    for (const [key, value] of map) {
        result.set(key, await callback(value, key));
    }
    return Promise.resolve(result);
};
exports.map = map;
const mapSynchronously = (_executionContext, map, callback) => {
    const result = new Map();
    map = (0, iterator_to_map_1.iteratorToMap)(map);
    for (const [key, value] of map) {
        result.set(key, callback(value, key));
    }
    return result;
};
exports.mapSynchronously = mapSynchronously;
