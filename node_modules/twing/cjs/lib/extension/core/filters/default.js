"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.defaultFilterSynchronously = exports.defaultFilter = void 0;
const is_empty_1 = require("../tests/is-empty");
const defaultFilter = (executionContext, value, defaultValue) => {
    return (0, is_empty_1.isEmpty)(executionContext, value)
        .then((isEmpty) => {
        if (isEmpty) {
            return Promise.resolve(defaultValue);
        }
        else {
            return Promise.resolve(value);
        }
    });
};
exports.defaultFilter = defaultFilter;
const defaultFilterSynchronously = (executionContext, value, defaultValue) => {
    if ((0, is_empty_1.isEmptySynchronously)(executionContext, value)) {
        return defaultValue;
    }
    return value;
};
exports.defaultFilterSynchronously = defaultFilterSynchronously;
